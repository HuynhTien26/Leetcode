Đề bài: Cho một mảng được sắp xếp gồm các số nguyên riêng biệt và một giá trị đích, trả về chỉ mục nếu tìm thấy mục tiêu. Nếu không, hãy trả về chỉ mục ở vị trí nếu nó được chèn theo thứ tự.
Ví dụ: 
Input nums = [1,3,5,6], target = 5
Output 2
Input: nums = [1,3,5,6], target = 2
Output: 1
Input: nums = [1,3,5,6], target = 7
Output: 4
Ý tưởng
- Dùng size để lấy số lượng phần tử trong nums
- Dùng vòng lặp for để xét từng phần tử 
  for(int i=0;i<n;i++)
  if(nums[i] <=target) return i;
  else return n;
